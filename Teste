//
// Teste de instruções JavaScript
//
function idade(num1) {
    if (num1 > 15 && num1 < 18 || num1 > 70) {
            return `O voto é opcional: ${num1}`
    }else if (num1 < 16){
            return `você não pode votar: ${num1}`
    } else{
        return `Você é obrigado a votar: ${num1}`
    }
}

for(let num2 = 1; num2 < 100; num2++){
    console.log(idade(num2))
}